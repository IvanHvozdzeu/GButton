📚 Описание библиотеки GButton

Класс GButton — это лёгкая и быстрая Arduino-библиотека для работы с кнопками.
Она позволяет определить:

одиночный клик

двойной клик

мультитап (3 и более кликов)

удержание (hold)

программный debounce

ожидание следующего клика

Все события определяются в режиме реального времени, без задержек delay() и с использованием millis(), что делает библиотеку подходящей для проектов с многозадачной логикой.

🚀 Как работает

Каждый вызов GetClickType() возвращает одно из значений:

Тип	Значение
NOCLICK	ничего не произошло
ONECLICK	одиночный клик
DOUBLECLICK	двойной клик
MULTICLICK	3 и более кликов
HOLD	кнопка удерживается
PRESSED	кнопка нажата, но событие ещё не определено
WAIT_NEXT_CLICK	ожидание второго/третьего клика
DEBOUNCE	антидребезг

При обнаружении завершённого события (клик/двойной/холд) функция возвращает соответствующий enum.

🔧 Используемые тайминги
#define HOLDING 300          // Время удержания для HOLD (мс)
#define DEBOUNCE_TIMER 10    // Антидребезг (мс)
#define MULTICLICK_TIMER 150 // Максимальная пауза между кликами (мс)

🧠 Алгоритм

Ждёт нажатия (NOCLICK → DEBOUNCE)

В режиме антидребезга подтверждает нажатие

Считает клики (ClickCount)

При отпускании ждёт второй / третий клик (WAIT_NEXT_CLICK)

Если кнопка удерживается > HOLDING → событие HOLD

Возвращает результат (ONECLICK, DOUBLECLICK, MULTICLICK, HOLD)
